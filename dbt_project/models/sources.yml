version: 2

sources:
  - name: dbo
    tables:
      - name: store_data
        meta:
          dagster:
            asset_key: ["store_data"] # This metadata specifies the corresponding Dagster asset for this dbt source.
